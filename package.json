{
  "name": "g3x-flugbuch-desktop",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "G3X Flight Log — 100% local desktop app to analyse Garmin G3X engine logs (Rotax 915 iS)",
  "author": "MrAirHD",
  "license": "UNLICENSED",
  "main": "electron/main.cjs",
  "scripts": {
    "start": "electron .",
    "dist": "electron-builder",
    "dist:win": "electron-builder --win nsis",
    "dist:mac": "electron-builder --mac dmg",
    "test": "node test/syntax-check.mjs app/index.html && node backend/selftest.mjs",
    "test:ui": "electron test/ui-smoke.cjs"
  },
  "dependencies": {
    "@fastify/multipart": "^9.0.3",
    "@fastify/static": "^8.1.1",
    "fastify": "^5.2.1"
  },
  "devDependencies": {
    "electron": "^33.2.1",
    "electron-builder": "^25.1.8"
  },
  "build": {
    "appId": "com.mrairhd.g3xflightlog",
    "productName": "G3X Flight Log",
    "artifactName": "G3X-Flight-Log-Setup-${version}.${ext}",
    "asar": false,
    "files": ["electron/**", "backend/**", "core/**", "app/**", "build/icon.ico", "node_modules/**", "package.json"],
    "directories": { "output": "release", "buildResources": "build" },
    "win": { "target": ["nsis"], "icon": "build/icon.ico" },
    "mac": {
      "target": [{ "target": "dmg", "arch": ["arm64", "x64"] }],
      "category": "public.app-category.utilities",
      "icon": "build/icon.png",
      "artifactName": "G3X-Flight-Log-${version}-${arch}.${ext}"
    },
    "dmg": { "title": "G3X Flight Log ${version}" },
    "nsis": {
      "oneClick": false,
      "perMachine": false,
      "allowToChangeInstallationDirectory": true,
      "installerIcon": "build/icon.ico",
      "uninstallerIcon": "build/icon.ico",
      "installerHeaderIcon": "build/icon.ico",
      "multiLanguageInstaller": true,
      "displayLanguageSelector": true,
      "installerLanguages": ["en_US", "de_DE"],
      "createDesktopShortcut": true,
      "createStartMenuShortcut": true,
      "shortcutName": "G3X Flight Log"
    }
  }
}
